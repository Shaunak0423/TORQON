// import type { NextConfig } from "next";

const nextConfig = {
  output: "export",

  basePath: "/TORQON",
  assetPrefix: "/TORQON/",

  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;

